
cmake_minimum_required(VERSION 3.5)
project(HACC NONE)

message(FATAL_ERROR "HACC has been migrated from C++ to the Python package 'hacc'. Install from the repository root with 'pip install -e .' or run the CLI via 'hacc'.")
